/**
 * Reserved fallback after source-specific terms and robots.txt approval.
 * RSS ingestion is deliberately preferred and is the only enabled Phase 1 fetcher.
 * Cheerio supports static HTML extraction; Playwright is reserved for permitted dynamic sites.
 */
export {};
